# Baizora is in private mode (2026-09-07). Disallow all crawling and indexing.
# To restore public crawling, revert this file (see git history for the prior
# per-page all/disallow list) and re-add the Sitemap line.
User-agent: *
Disallow: /
